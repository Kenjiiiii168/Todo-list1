# 🔧 แก้ Postgres Service: failed to exec pid1

## ❌ Error ที่เจอ:

```
ERROR (catatonit:2): failed to exec pid1: No such file or directory
```

ใน Postgres service (Database service)

## 🔍 สาเหตุ:

Postgres service เป็น **managed service** (Railway จัดการให้อัตโนมัติ) ไม่ควรมี Start Command หรือ Custom Configuration

## ✅ วิธีแก้:

### ตรวจสอบ Postgres Service Settings:

1. ไปที่ **Postgres service** → **Settings** tab
2. **ตรวจสอบว่าไม่มี Start Command:**
   - หา **"Custom Start Command"** หรือ **"Start Command"** field
   - ถ้ามี → **ลบออก** (ให้ว่างเปล่า)
   - **Save**

3. **ตรวจสอบว่าไม่มี Build Command:**
   - หา **"Custom Build Command"** หรือ **"Build Command"** field
   - ถ้ามี → **ลบออก** (ให้ว่างเปล่า)
   - **Save**

4. **ตรวจสอบ Root Directory:**
   - หา **"Root Directory"** field
   - ถ้ามี → **ลบออก** (ให้ว่างเปล่า)
   - **Save**

**หมายเหตุ:** Postgres service เป็น managed service - Railway จะจัดการให้อัตโนมัติ ไม่ต้องตั้งค่า Build/Start Command

---

## 📝 Checklist:

- [ ] ลบ Start Command จาก Postgres service (ถ้ามี)
- [ ] ลบ Build Command จาก Postgres service (ถ้ามี)
- [ ] ลบ Root Directory จาก Postgres service (ถ้ามี)
- [ ] Deploy ใหม่ (หรือรอให้ Railway restart อัตโนมัติ)

---

## 🆘 ถ้ายังไม่ได้:

### วิธีที่ 1: ลบ Postgres Service แล้วสร้างใหม่

1. ลบ Postgres service ปัจจุบัน
2. สร้างใหม่:
   - คลิก **"+ New"** → **"Database"** → **"PostgreSQL"**
   - Railway จะสร้างให้อัตโนมัติ (ไม่ต้องตั้งค่าอะไร)

### วิธีที่ 2: ตรวจสอบว่า Postgres Service ถูกสร้างเป็น Database

1. ตรวจสอบว่า Postgres service เป็น **"Database"** type (ไม่ใช่ "Service")
2. ถ้าเป็น "Service" → ลบแล้วสร้างใหม่เป็น "Database"

---

## 💡 สรุป:

**ปัญหา:** Postgres service มี Start Command หรือ Build Command ที่ไม่ควรมี (เพราะเป็น managed service)

**วิธีแก้:** ลบ Start Command, Build Command, และ Root Directory ออกจาก Postgres service

ลองทำตามนี้ แล้วบอกผลลัพธ์!

