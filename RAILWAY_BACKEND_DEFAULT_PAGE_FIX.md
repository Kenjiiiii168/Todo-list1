# 🔧 แก้ Backend URL แสดงหน้า Default ของ Railway

## ❌ ปัญหาที่เจอ:

Backend URL (เช่น `https://your-backend.railway.app`) แสดงหน้า default ของ Railway:
- ASCII art ของรถไฟ
- "✨ Home of the Railway API ✨"

แทนที่จะเป็น API response: `{"status": "ok", "service": "todo-api"}`

## 🔍 สาเหตุ:

1. **Backend service ยังไม่ได้ start** - Start Command ไม่ถูกต้องหรือมีปัญหา
2. **Backend service ไม่ได้ listen บน PORT** - ไม่ได้ bind กับ `$PORT`
3. **Backend service crash** - มี error ใน application

## ✅ วิธีแก้:

### ขั้นตอนที่ 1: ตรวจสอบ Backend Service Status

1. ไปที่ **Backend service** → **Deployments** tab
2. ดูว่า deployment ล่าสุด:
   - ✅ **"Active"** (สีเขียว) = service กำลังรันอยู่
   - ❌ **"Failed"** (สีแดง) = service มีปัญหา

### ขั้นตอนที่ 2: ตรวจสอบ Deploy Logs

1. ไปที่ **Backend service** → **Deploy Logs** tab
2. ดูว่า:
   - Container start สำเร็จหรือไม่
   - มี error อะไรหรือไม่
   - Gunicorn start สำเร็จหรือไม่

### ขั้นตอนที่ 3: ตรวจสอบ Start Command

1. ไปที่ **Backend service** → **Settings** tab
2. **เลื่อนลงไปดูส่วน "Deploy"**
3. หา **"Custom Start Command"** field
4. **ตรวจสอบ Start Command:**
   - ควรเป็น: `cd Login && python3 -m gunicorn wsgi:application --bind 0.0.0.0:$PORT --workers 2`
   - **หมายเหตุ:** 
     - ใช้ `cd Login` ไม่ใช่ `cd server/Login` (เพราะ Root Directory = `server` แล้ว)
     - ใช้ `python3` (Nixpacks ใช้ `python3`)
     - ใช้ `--bind 0.0.0.0:$PORT` (สำคัญมาก!)
5. **Save**

### ขั้นตอนที่ 4: ตรวจสอบ HTTP Logs

1. ไปที่ **Backend service** → **HTTP Logs** tab
2. ดูว่า:
   - มี request เข้ามาหรือไม่
   - มี response อะไรหรือไม่
   - มี error อะไรหรือไม่

---

## 📝 Checklist:

- [ ] ตรวจสอบ Backend service status (Active หรือ Failed)
- [ ] ตรวจสอบ Deploy Logs ว่าไม่มี error
- [ ] ตรวจสอบ Start Command ว่าใช้ `--bind 0.0.0.0:$PORT`
- [ ] ตรวจสอบ HTTP Logs ว่ามี request/response หรือไม่

---

## 🆘 ถ้ายังไม่ได้:

### ตรวจสอบ Environment Variables:

1. ไปที่ **Backend service** → **Variables** tab
2. ตรวจสอบว่า:
   - `DATABASE_URL` ถูกตั้งค่าแล้ว
   - `SECRET_KEY` ถูกตั้งค่าแล้ว
   - `FLASK_APP=app.py` ถูกตั้งค่าแล้ว

### ตรวจสอบ Build Logs:

1. ไปที่ **Backend service** → **Build Logs** tab
2. ดูว่า:
   - Build สำเร็จหรือไม่
   - `pip install` ทำงานหรือไม่
   - gunicorn ถูกติดตั้งหรือไม่

### ลอง Restart Service:

1. ไปที่ **Backend service** → **Settings** tab
2. หา **"Restart"** button หรือ **"Redeploy"** button
3. คลิกเพื่อ restart service

---

## 💡 สรุป:

**ปัญหา:** Backend service ยังไม่ได้ start หรือ Start Command ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ Start Command: `cd Login && python3 -m gunicorn wsgi:application --bind 0.0.0.0:$PORT --workers 2`
2. ตรวจสอบ Deploy Logs ว่ามี error หรือไม่
3. ตรวจสอบ HTTP Logs ว่ามี request/response หรือไม่

ลองทำตามนี้ แล้วบอกผลลัพธ์!

