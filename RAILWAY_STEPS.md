# 🚂 Railway Step-by-Step Guide (แบบละเอียด)

## 📍 ขั้นตอนที่ 3-5: Run Migration และทดสอบ

### 🔍 หา "Deployments" tab และ "Open Shell"

**วิธีที่ 1: ใช้ Deployments Tab (ถ้ามี)**

1. ไปที่ **Backend service** (คลิกที่ชื่อ service)
2. ดูที่ **tabs ด้านบน**:
   - **Variables** (ตัวแปร)
   - **Settings** (ตั้งค่า)
   - **Deployments** (การ deploy) ← **คลิกที่นี่**
   - **Metrics** (สถิติ)
   - **Logs** (log)
3. ใน **Deployments tab**:
   - จะเห็นรายการ deployments
   - คลิกที่ deployment ล่าสุด (ที่ deploy เสร็จแล้ว)
   - ควรมีปุ่ม **"..."** หรือ **"More"** หรือ **"Open Shell"**

**ถ้าหาไม่เจอ → ใช้วิธีที่ 2**

---

### 🔍 วิธีที่ 2: ใช้ Logs Tab + Manual Command

1. ไปที่ **Backend service** → **Logs** tab
2. ดูว่า service กำลังรันอยู่หรือไม่
3. **ตั้งค่า Pre-Deploy Command** (แนะนำ - ง่ายกว่า):
   - ไปที่ **Settings** tab
   - หา **"Pre-Deploy Command"** หรือ **"Before Deploy"**
   - ใส่: `cd server/Login && FLASK_APP=app.py python3 -m flask db upgrade`
   - หรือ: `cd server/Login && export FLASK_APP=app.py && python3 -m flask db upgrade`
   - **หมายเหตุ:** ใช้ `python3 -m flask` แทน `flask` เพื่อให้แน่ใจว่าใช้ Python ที่ถูกต้อง

---

### 🔍 หา "Public Domain" (URL ของ Backend)

1. ไปที่ **Backend service** → **Settings** tab
2. หา **"Networking"** section หรือ **"Domains"** section
3. ควรเห็น:
   - **"Public Domain"** หรือ
   - **"Custom Domain"** หรือ
   - **"Generate Domain"** (คลิกเพื่อสร้าง)
4. Copy URL ที่ได้ (เช่น `https://your-backend.railway.app`)

**ถ้าหาไม่เจอ:**
- ไปที่ **Project** → **Settings** → **"Networking"**
- หรือดูที่ **top bar** ของ service (อาจแสดง URL อยู่)

---

### 🔍 วิธีทดสอบ Backend

**วิธีที่ 1: ใช้ Browser**
1. Copy URL จาก "Public Domain"
2. เปิดใน browser
3. ควรเห็น: `{"status": "ok", "service": "todo-api"}`

**วิธีที่ 2: ใช้ curl (ใน Terminal)**
```bash
curl https://your-backend.railway.app
```

**วิธีที่ 3: ใช้ Postman/Insomnia**
- GET request ไปที่ URL
- ควรได้ response: `{"status": "ok", "service": "todo-api"}`

---

## 🆘 ถ้ายังหาไม่เจอ

### ตรวจสอบว่า:
1. **Backend service deploy เสร็จแล้วหรือยัง?**
   - ดูที่ **Logs** tab → ควรเห็น "Application startup complete"
   - หรือดูที่ **Deployments** tab → ควรเห็น deployment ที่สำเร็จ (สีเขียว)

2. **มี tabs อะไรบ้าง?**
   - บอกฉันมาว่ามี tabs อะไรบ้าง (Variables, Settings, Logs, etc.)

3. **Railway UI ดูเป็นยังไง?**
   - มี sidebar ด้านซ้ายหรือไม่?
   - มี menu ด้านบนหรือไม่?

---

## 💡 วิธีง่ายที่สุด: ใช้ Pre-Deploy Command

**แทนที่จะ run migration แยก ให้ตั้งค่าให้ run อัตโนมัติ:**

1. ไปที่ **Backend service** → **Settings** tab
2. หา **"Pre-Deploy Command"** หรือ **"Before Deploy"**
3. ใส่:
   ```
   cd server/Login && FLASK_APP=app.py python3 -m flask db upgrade
   ```
   หรือ:
   ```
   cd server/Login && export FLASK_APP=app.py && python3 -m flask db upgrade
   ```
4. **Save**
5. Railway จะ run migration อัตโนมัติทุกครั้งที่ deploy ✅

---

## 📸 ถ้ายังหาไม่เจอ

**ส่ง screenshot มาได้** หรือบอกฉันว่า:
- มี tabs อะไรบ้าง?
- มี menu อะไรบ้าง?
- UI ดูเป็นยังไง?

ฉันจะช่วยหาตรงที่ถูกต้องให้!

